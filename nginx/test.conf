upstream client {
    server frontend:3000; # front: reactjs container name
}

upstream back {
    server backend:5000; # back: springboot container name
}

server {
    listen 80;
    server_name stackers.site;
    # server_tokens off;

    # location /.well-known/acme-challenge/ {
    #     allow all;
    #     root /var/www/certbot;
    # }

    # location / {
    #     return 301 https://$host$request_uri;
    # }

# }
# server {
    # listen 443 ssl;
    # server_name localhost; #i8c210.p.ssafy.io; # 도메인으로 변경
    # server_tokens off;

    # ssl_certificate /etc/letsencrypt/live/localhost/fullchain.pem; # example.org를 도메인으로 변경
    # ssl_certificate_key /etc/letsencrypt/live/localhost/privkey.pem; # example.or를 도메인으로 변경
    # include /etc/letsencrypt/options-ssl-nginx.conf;
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {        
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_pass http://client;
    }

    location /api {
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_pass http://back/api;
    }
}
